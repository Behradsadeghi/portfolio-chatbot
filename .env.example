# Copy this file to `.env` and paste your real key there.
# `.env` is gitignored; this `.env.example` is committed so anyone cloning
# the repo knows which variables they need to set.

GEMINI_API_KEY=paste-your-key-here
